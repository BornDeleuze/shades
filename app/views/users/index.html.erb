<h2>Here's a lit of users. Click on them to check out what books they have!
</h2>
<ul>
<% @users.each do |user|%>
    <li><%= link_to user.username, "/users/#{user.id}/user_books" %></li>
<% end %>
</ul>
<%= link_to "home", "/"%>